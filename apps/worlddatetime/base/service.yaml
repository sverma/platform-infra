apiVersion: v1
kind: Service
metadata:
  name: worlddatetime
  namespace: worlddatetime

  labels:
    app: worlddatetime
    
spec:
  selector:
    app: worlddatetime

  ports:
    - name: http
      port: 80
      targetPort: 8080
