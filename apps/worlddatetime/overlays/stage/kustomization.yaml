apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: worlddatetime

resources:
  - ../../base

patches:
  - path: replicas.yaml
  - path: image.yaml
  - path: gateway-host.yaml
  - path: resource-limits.yaml

