apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: weatherserver
spec:
  podSelector:
    matchLabels:
      app: weatherserver

  policyTypes:
    - Ingress
    - Egress

  ingress:
    - from:
        - namespaceSelector: {}

  egress:
    - {}
