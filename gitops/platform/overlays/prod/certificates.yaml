apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: certificates-prod
  namespace: argocd

spec:

  project: default

  source:
    repoURL: https://github.com/sverma/platform-infra.git
    targetRevision: main
    path: gitops/platform/base/certificates

  destination:
    server: https://34.93.253.26
    namespace: envoy-gateway-system

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
