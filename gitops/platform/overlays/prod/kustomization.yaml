apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization


resources:
  - ../../base
  - cert-manager.yaml
  - gateways.yaml 

patches:
  - path: envoy-gateway-patch.yaml
  - path: external-dns-patch.yaml