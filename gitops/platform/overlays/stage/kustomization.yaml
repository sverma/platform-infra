apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization


resources:
  - cert-manager.yaml
  - gateways.yaml

nameSuffix: -stage

patches:
  - path: envoy-gateway-patch.yaml
  - path: external-dns-patch.yaml
