apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: weatherserver-stage
  namespace: argocd

spec:
  project: apps

  source:
    repoURL: https://github.com/sverma/platform-infra.git
    targetRevision: main
    path: apps/weatherserver/overlays/stage

  destination:
    server: https://kubernetes.default.svc
    namespace: weatherserver

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true