apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: weatherserver-prod
  namespace: argocd

spec:
  project: apps-prod

  source:
    repoURL: https://github.com/sverma/platform-infra.git
    targetRevision: main
    path: apps/weatherserver/overlays/prod

  destination:
    server: https://34.93.253.26
    namespace: weatherserver

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true